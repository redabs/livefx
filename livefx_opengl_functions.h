PROC(gl_create_shader, glCreateShader);
PROC(gl_create_program, glCreateProgram);
PROC(gl_attach_shader, glAttachShader);
PROC(gl_detach_shader, glDetachShader);
PROC(gl_link_program, glLinkProgram);
PROC(gl_compile_shader, glCompileShader);
PROC(gl_get_shader_iv, glGetShaderiv);
PROC(gl_get_shader_info_log, glGetShaderInfoLog);
PROC(gl_get_program_info_log, glGetProgramInfoLog);
PROC(gl_get_program_iv, glGetProgramiv);
PROC(gl_shader_source, glShaderSource);
PROC(gl_use_program, glUseProgram);
PROC(gl_gen_buffers, glGenBuffers);
PROC(gl_gen_vertex_arrays, glGenVertexArrays);
PROC(gl_bind_buffer, glBindBuffer);
PROC(gl_buffer_data, glBufferData);
PROC(gl_buffer_sub_data, glBufferSubData);
PROC(gl_enable_vertex_attrib_array, glEnableVertexAttribArray);
PROC(gl_disable_vertex_attrib_array, glDisableVertexAttribArray);
PROC(gl_vertex_attrib_pointer, glVertexAttribPointer);
PROC(gl_vertex_attrib_divisor, glVertexAttribDivisor);
PROC(gl_vertex_attrib_i_pointer, glVertexAttribIPointer);
//PROC(gl_active_texture, glActiveTexture);
PROC(gl_bind_framebuffer, glBindFramebuffer);
PROC(gl_bind_renderbuffer, glBindRenderbuffer);
PROC(gl_renderbuffer_storage, glRenderbufferStorage);
PROC(gl_gen_renderbuffers, glGenRenderbuffers);
PROC(gl_framebuffer_texture_2d, glFramebufferTexture2D);
PROC(gl_gen_framebuffers, glGenFramebuffers);
PROC(gl_framebuffer_renderbuffer, glFramebufferRenderbuffer);
PROC(gl_check_framebuffer_status, glCheckFramebufferStatus);
PROC(gl_get_uniform_uiv, glGetUniformuiv);
PROC(gl_uniform_1i, glUniform1i);
PROC(gl_uniform_1ui, glUniform1ui);
PROC(gl_uniform_1f, glUniform1f);
PROC(gl_uniform_2f, glUniform2f);
PROC(gl_uniform_3f, glUniform3f);
PROC(gl_uniform_4f, glUniform4f);
PROC(gl_uniform_4fv, glUniform4fv);
PROC(gl_uniform_matrix_4fv, glUniformMatrix4fv);
PROC(gl_get_uniform_location, glGetUniformLocation);
PROC(gl_get_attrib_location, glGetAttribLocation);
PROC(gl_bind_attrib_location, glBindAttribLocation);
PROC(gl_draw_buffers, glDrawBuffers);
PROC(gl_delete_buffers, glDeleteBuffers);
PROC(gl_get_buffer_parameter_iv, glGetBufferParameteriv);
PROC(gl_gen_samplers, glGenSamplers);
PROC(gl_bind_sampler, glBindSampler);
PROC(gl_bind_vertex_array, glBindVertexArray);
PROC(gl_primitive_restart_index, glPrimitiveRestartIndex);
PROC(gl_draw_elements_instanced, glDrawElementsInstanced);
PROC(gl_delete_shader, glDeleteShader);
PROC(gl_delete_program, glDeleteProgram);
PROC(gl_get_string_i, glGetStringi);
